import React from "react";

const Day = ({ day }) => {
    return (
        <div className="container">
            <span className="dayText">{day}</span>
        </div>
    );
};

export default Day;
